#!/bin/bash
set -e

# install Node dependencies
npm install

# optional: install TypeScript globally if needed
# npm install -g typescript

# you can add other tools here (PostgreSQL client, etc.)
chmod +x setup.sh
